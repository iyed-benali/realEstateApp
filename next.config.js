/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains:['wp-tid.zillowstatic.com',
    "media.istockphoto.com"]
  }
}

module.exports = nextConfig
